FUNCTION "XSK_TEST_APP"."xsk-test-app::usersTableFunction" ()
	RETURNS TABLE (
		"ID" NVARCHAR(32),
		"Name" NVARCHAR(32)
	)
	LANGUAGE SQLSCRIPT
	SQL SECURITY INVOKER AS
	
BEGIN

	RETURN  SELECT "ID", "Name" FROM "xsk-test-app::usersTable" WHERE SESSION_CONTEXT(APPLICATIONUSER) = 'APPUSER';

END;